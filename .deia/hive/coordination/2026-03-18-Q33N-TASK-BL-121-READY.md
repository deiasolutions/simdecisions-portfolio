# Q33N Task Files Ready for Review: BL-121 Properties Panel Editing

**Date:** 2026-03-18
**From:** Q33N (Queen Coordinator)
**To:** Q88NR (Regent)
**Briefing:** BL-121 Properties Panel Port

---

## Summary

Three task files created to implement properties panel editing for the Canvas EGG:

1. **TASK-BL-121-A:** Add inline editing UI to TreeBrowser (frontend UI layer)
2. **TASK-BL-121-B:** Wire property changes to Canvas (bus subscription + state update)
3. **TASK-BL-121-C:** Integration test (end-to-end verification)

All tasks are independent and can be dispatched in parallel. Dependencies are soft: the integration test (C) will validate that A+B work together correctly.

---

## Task Files Created

### TASK-BL-121-A: Tree Browser Inline Editing UI

**File:** `C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\.deia\hive\tasks\2026-03-18-TASK-BL-121-A-tree-browser-inline-editing.md`

**Deliverables:**
- Add `editable` flag to TreeNodeData meta type
- Add inline edit mode to TreeNodeRow (click to edit, blur/Enter saves, Escape cancels)
- Input type inference: string → text input, number → number input, object → JSON text
- CSS for edit mode (`.tree-node-row.editing`, `.tree-node-input`)
- Update propertiesAdapter to mark properties as editable (name, timing, operator)
- Wire `onPropertyChange` callback from TreeBrowser to adapter

**Tests:** 9+ tests covering edit modes, save/cancel, keyboard handlers, type inference

**Files modified:** 6 files
- `browser/src/primitives/tree-browser/types.ts`
- `browser/src/primitives/tree-browser/TreeNodeRow.tsx`
- `browser/src/primitives/tree-browser/TreeBrowser.tsx`
- `browser/src/primitives/tree-browser/adapters/propertiesAdapter.ts`
- `browser/src/primitives/tree-browser/tree-browser.css`
- `browser/src/primitives/tree-browser/__tests__/TreeNodeRow.test.tsx` (new)

**Constraints:**
- TDD (tests first)
- No file over 500 lines
- CSS: `var(--sd-*)` only
- No stubs

---

### TASK-BL-121-B: Canvas Property Subscription

**File:** `C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\.deia\hive\tasks\2026-03-18-TASK-BL-121-B-canvas-property-subscription.md`

**Deliverables:**
- Add `setNestedValue(obj, path, value)` helper for dot-separated paths
- Subscribe to `properties:value-changed` in CanvasApp
- On property change: find node, clone data, update via setNestedValue, apply via setNodes
- Publish `canvas:node-updated` after applying change
- Handle edge cases: invalid node ID, missing fields, nested paths

**Tests:** 9+ tests covering nested paths, node updates, event publishing, error handling

**Files modified:** 3 files
- `browser/src/primitives/canvas/CanvasApp.tsx`
- `browser/src/infrastructure/relay_bus/types/messages.ts` (if types missing)
- `browser/src/primitives/canvas/__tests__/CanvasApp.propertyUpdates.test.tsx` (new)

**Constraints:**
- TDD (tests first)
- No file over 500 lines
- No stubs
- ReactFlow node.data structure (not top-level node)

---

### TASK-BL-121-C: Integration Test

**File:** `C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\.deia\hive\tasks\2026-03-18-TASK-BL-121-C-integration-test.md`

**Deliverables:**
- Create end-to-end test file: `canvas-properties-integration.test.tsx`
- Test: select node → properties populate
- Test: edit property → canvas updates
- Test: nested property edit works
- Test: deselect → properties clear
- Test: select different node → properties switch
- Test: invalid node ID ignored gracefully

**Tests:** 7+ integration tests covering full round-trip

**Files created:** 1 file
- `browser/src/primitives/canvas/__tests__/canvas-properties-integration.test.tsx`

**Constraints:**
- TDD (tests first)
- File under 500 lines
- Use real MessageBus (not mocked)
- Use real propertiesAdapter (not mocked)

---

## Dispatch Plan

All three tasks are independent and can run in parallel:

1. **TASK-BL-121-A** → Haiku (UI implementation, straightforward)
2. **TASK-BL-121-B** → Haiku (bus subscription, state update)
3. **TASK-BL-121-C** → Haiku (integration tests, verification)

**Rationale for Haiku:**
- All tasks have clear, concrete deliverables
- TDD enforces correctness (tests define behavior)
- No architectural ambiguity
- Haiku is fast and cost-effective for well-specified tasks

**Dependencies:**
- Soft: TASK-BL-121-C validates that A+B work together
- Hard: None (all tasks can start immediately)

**Slot allocation:**
- All 3 tasks fit in 3 bee slots
- Total estimated time: ~60-90 minutes wall time (parallel)

---

## Test Coverage Summary

| Task | Test File | Test Count | Coverage |
|------|-----------|------------|----------|
| BL-121-A | TreeNodeRow.test.tsx | 9+ | Inline editing UI, keyboard handlers, save/cancel |
| BL-121-B | CanvasApp.propertyUpdates.test.tsx | 9+ | Bus subscription, nested paths, node updates |
| BL-121-C | canvas-properties-integration.test.tsx | 7+ | Full round-trip, select/edit/update/deselect |
| **Total** | **3 test files** | **25+** | **Full feature coverage** |

---

## Acceptance Criteria (from BL-121 spec)

- [ ] Selecting a canvas node shows its properties in the panel
- [ ] Editing a property updates the canvas node
- [ ] Deselecting clears the panel
- [ ] Tests pass
- [ ] No hardcoded colors (all CSS uses `var(--sd-*)`)
- [ ] No file over 500 lines
- [ ] No stubs or TODO comments shipped

---

## Next Steps

**For Q88NR:**
1. Review the three task files
2. Check for missing deliverables, vague acceptance criteria, or constraint violations
3. If corrections needed, tell Q33N what to fix
4. If approved, authorize dispatch

**After approval:**
- Q33N will dispatch all three tasks in parallel (3 bees)
- Q33N will monitor responses and review outputs
- Q33N will report completion to Q88NR

---

## Notes

1. **No hardcoded colors:** All task files specify CSS must use `var(--sd-*)` only.
2. **File size limits:** All tasks constrained to 500 lines max, with modularization guidance.
3. **TDD enforced:** All tasks require tests FIRST, then implementation.
4. **No stubs:** All tasks explicitly forbid shipping stubs or TODOs.
5. **Absolute paths:** All file paths in task files are absolute Windows paths.
6. **Response format:** All tasks include mandatory 8-section response template.

---

**End of Report**
