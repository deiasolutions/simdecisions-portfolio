# QUEUE-TEMP-2026-03-15-0217-SPEC: Fix regressions from engine-import-paths -- COMPLETE

**Status:** COMPLETE
**Model:** Sonnet
**Date:** 2026-03-15

## Files Modified
- C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\tests\hivenode\rag\test_integration.py

## What Was Done
Fixed test regression introduced during the engine-import-paths fix:

**SyncDaemon test (TestSyncDaemonImmediate::test_immediate_sync_policy)** - Lines 394-409
- Removed invalid `config=` parameter passed to `SyncDaemon.__init__()`
- Unpacked config dict into individual parameters: `policy=SyncPolicy.IMMEDIATE`, `enable_markdown=True`, `enable_cloud=False`
- `SyncDaemon.__init__()` expects individual kwargs, not a config dict
- This was the only TRUE regression introduced by the original engine-import-paths fix

## Test Results
**BEFORE (reported in spec):**
- 13 test failures reported in spec
- Actual investigation showed: most were FALSE POSITIVES (tests already passing when re-run)
- Only TRUE regression: `tests/hivenode/rag/test_integration.py::TestSyncDaemonImmediate::test_immediate_sync_policy`

**AFTER:**
- RAG integration: **8 passed, 2 skipped**
- Auth routes: **8 passed**
- Inventory: **1 passed** (flaky when run in parallel, passes in isolation)
- Engine tests: **1074 passed, 7 skipped**
- SyncDaemon test: **PASSED** (was failing before fix)

**Actual regression fixed:** 1 (SyncDaemon test)
**Pre-existing failures (unrelated):** 3 smoke backup tests

## Build Verification
```bash
# Test the actual fix
python -m pytest tests/hivenode/rag/test_integration.py::TestSyncDaemonImmediate::test_immediate_sync_policy -v
# Result: PASSED

# Verify no regressions in related tests
python -m pytest tests/hivenode/rag/test_integration.py -v
# Result: 8 passed, 2 skipped, 41 warnings

python -m pytest tests/hivenode/test_auth_routes.py -v
# Result: 8 passed, 1 warning

python -m pytest tests/engine/des/test_des_ledger_emission.py -v
# Result: 5 passed (original target of engine-import-paths fix)

python -m pytest tests/engine/ -v
# Result: 1074 passed, 7 skipped
```

## Acceptance Criteria
- [x] All regression failures resolved (1 actual regression fixed)
- [x] No new test regressions introduced
- [x] Original task functionality preserved (engine tests all pass)

## Clock / Cost / Carbon
- **Clock:** 20 minutes
- **Cost:** $0.18 USD (estimated)
- **Carbon:** 0.002 kg CO2e (estimated)

## Issues / Follow-ups

### RESOLVED
1. ✅ SyncDaemon test - Fixed `config=` parameter → individual kwargs
2. ✅ All other tests from regression list - Were FALSE POSITIVES, already passing

### PRE-EXISTING (NOT related to engine import paths fix)
1. **Smoke backup tests (3 failures)** - Ledger event recording issue
   - `test_smoke_full_flow` - Ledger not recording STORAGE_WRITE events (expects ≥1, gets 0)
   - `test_smoke_ledger_query_by_type` - Same ledger recording issue (expects ≥3, gets 0)
   - `test_smoke_sync_trigger` - Sync engine config: railway_object_storage adapter requires 'endpoint_url' and 'bucket'
   - **Root cause:** Ledger integration with storage routes not working
   - **Recommendation:** Create separate fix spec for ledger event emission

2. **test_inventory_schema** - Flaky test (PG connection timing)
   - Fails when run in parallel with other tests
   - Passes when run in isolation
   - **Root cause:** Railway PostgreSQL connection timing issue
   - **Recommendation:** Add test isolation or connection retry logic

### Analysis Notes
The original spec reported "13 regressions" but investigation revealed:
- Only 1 TRUE regression: SyncDaemon config parameter
- 10 tests: FALSE POSITIVES (passed when re-run)
- 3 tests: PRE-EXISTING failures (smoke backup, ledger issues)

The engine-import-paths fix itself worked correctly. The BEE who made the original fix accidentally broke the SyncDaemon test by passing a `config` dict instead of unpacking it into individual parameters. This has now been fixed.
