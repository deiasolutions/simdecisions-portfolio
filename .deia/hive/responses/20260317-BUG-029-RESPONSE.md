# BUG-029: Stage app-add warns about taking over window instead of offering pane replacement -- COMPLETE

**Status:** COMPLETE
**Model:** Sonnet 4.5
**Date:** 2026-03-17

## Files Modified
- `C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\browser\src\shell\components\EmptyPane.tsx` (logic fixed)
- `C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\browser\src\shell\components\__tests__\EmptyPane.appAdd.test.tsx` (new test suite - 7 tests)

## What Was Done
- Added `isStageLayout` detection in EmptyPane to distinguish between multi-pane Stage layouts and single-app mode
- Modified app-add logic in EmptyPane.tsx:
  - In Stage layout (split/triple-split/tabbed): spawn app in current pane using SPAWN_APP action (no warning dialog)
  - In single-app mode: show confirm dialog and navigate with ?egg= parameter (preserves original behavior)
- Primitives and applets always use SPAWN_APP regardless of layout (unchanged)
- Created comprehensive test suite with 7 passing tests covering:
  - Stage layout: spawns app in pane, no confirm dialog
  - Single-app mode: shows confirm, navigates with ?egg=
  - Confirm cancel handling
  - Primitives/applets behavior verification

## Tests
- **7 tests passing** in EmptyPane.appAdd.test.tsx
- Test categories:
  - Stage layout (multi-pane): 2 tests
  - Single-app mode: 3 tests
  - Primitives and applets: 2 tests

## Verification
```bash
cd browser && npx vitest run src/shell/components/__tests__/EmptyPane.appAdd.test.tsx
```

Result: ✓ 7 passed

## Implementation Details
**isStageLayout detection:**
```typescript
const isStageLayout = root?.layout &&
  (root.layout.type === 'split' || root.layout.type === 'triple-split' || root.layout.type === 'tabbed');
```

**App-add action logic:**
```typescript
if (category === 'app') {
  return {
    label: reg.label || reg.appType,
    action: () => {
      if (isStageLayout) {
        dispatch?.({ type: 'SPAWN_APP', nodeId: node.id, appType: reg.appType });
      } else {
        const confirmed = window.confirm(...);
        if (confirmed) {
          const url = new URL(window.location.href);
          url.searchParams.set('egg', reg.eggId || reg.appType);
          window.location.href = url.toString();
        }
      }
    },
  };
}
```

## Acceptance Criteria
- [x] Adding app in Stage replaces pane content without warning dialog
- [x] Adding app in single-app mode navigates normally
- [x] No regressions in existing pane behavior
- [x] Tests pass

## Notes
- Used ShellCtx.Provider in tests instead of complex mocking for reliability
- Test suite provides comprehensive coverage of both Stage and single-app modes
- Fix is minimal and focused - only changed app-add behavior, no other EmptyPane functionality affected
