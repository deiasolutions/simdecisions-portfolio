# Q33N → Q33NR: TASK-244 Ready for Review

**Task:** TASK-244 (Wire LandingPage route into App.tsx)
**Q33N Bot ID:** QUEEN-2026-03-18-BRIEFING-TASK-244-l
**Date:** 2026-03-18

---

## Task File Created

**Location:** `C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\.deia\hive\tasks\2026-03-18-TASK-244-LANDING-PAGE-ROUTE.md`

## Summary

I have created ONE task file for a bee to wire the existing LandingPage component into App.tsx.

### What the Bee Will Do

**File to modify:** `browser/src/App.tsx` (currently 102 lines, will stay under 150)

**Changes required:**
1. Import `LandingPage` from `'./pages/LandingPage'`
2. Add helper function `shouldShowLandingPage()` to check for landing page condition:
   - No `?egg=` parameter in URL
   - AND empty pathname (just `/`)
3. Conditionally render:
   - If landing page condition → render `<LandingPage />`
   - Otherwise → existing Shell flow (no changes)

**Preserved behavior:**
- OAuth token extraction (`extractTokenFromUrl()`) still runs first
- All Shell loading/error states unchanged
- All existing EGG routing still works (`?egg=canvas`, `/chat`, etc.)

### Test Requirements

The bee will run:
1. LandingPage component tests (verify no regressions)
2. Full frontend test suite (`cd browser && npx vitest run`)
3. Verify zero new test failures

### What the Bee Will NOT Do

- Recreate LandingPage.tsx (already exists)
- Recreate LandingPage.css (already exists)
- Modify useEggInit, eggResolver, or any egg loading logic
- Change existing Shell rendering logic

## Task File Checklist

I have verified the task file includes:
- [x] Clear objective (one sentence)
- [x] Context explaining the problem and solution
- [x] All file paths are absolute Windows paths
- [x] "Files to Read First" section with full paths
- [x] "Files to Modify" section with full paths
- [x] Concrete deliverables with checkboxes
- [x] Test requirements with specific commands
- [x] Implementation notes with pseudocode
- [x] Constraints (no stubs, CSS vars only, 500-line limit)
- [x] Response requirements (8 sections mandatory)
- [x] Model assignment (sonnet)
- [x] Priority (P2)

## Task File Quality

- **No stubs:** All deliverables are concrete and testable
- **No hardcoded colors:** Constraint explicitly stated, already satisfied by existing CSS
- **File size:** App.tsx will stay under 150 lines (currently 102)
- **TDD:** Tests run after implementation to verify no regressions
- **Absolute paths:** All file paths use full Windows paths
- **Clear acceptance criteria:** 3 categories with 11 checkboxes total

## Dispatch Plan (After Q33NR Approval)

**Model:** sonnet (routing logic requires careful testing)
**Command:**
```bash
python .deia/hive/scripts/dispatch/dispatch.py .deia/hive/tasks/2026-03-18-TASK-244-LANDING-PAGE-ROUTE.md --model sonnet --role bee --inject-boot
```

**No timeout:** Let the bee run to natural completion (small task, ~15-20 minutes)

---

## Q33N Status: WAITING FOR Q33NR REVIEW

I have completed my task file creation. I am ready to dispatch the bee once Q33NR reviews and approves this task file.

**Next step:** Q33NR reviews task file and either:
1. Approves → I dispatch the bee
2. Requests corrections → I fix and resubmit

---

**Q33N signing off. Awaiting Q33NR approval.**
