# VERIFY-009: BL-208 — App Directory Sort Order

**Expected change date:** 2026-03-17 23:42
**Verification date:** 2026-03-18
**Verifier:** Verification Bee

---

## Files Checked

1. `C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\browser\src\primitives\apps-home\AppsHome.tsx`
2. `C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\browser\src\primitives\apps-home\AppsHome.css`
3. `C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\browser\src\primitives\apps-home\__tests__\AppsHome.test.tsx`

---

## Status

**MISSING**

---

## Evidence

### 1. Sort Logic — NOT PRESENT

**Expected:** AppsHome.tsx lines 51-58 should contain sort logic within `groupedEggs` useMemo:
```typescript
Object.keys(groups).forEach((section) => {
  groups[section].sort((a, b) => {
    const aVal = a.status === 'BUILT' ? 0 : 1;
    const bVal = b.status === 'BUILT' ? 0 : 1;
    return aVal - bVal;
  });
});
```

**Actual:** The `groupedEggs` useMemo (lines 38-52) contains NO sort logic. It only groups eggs by section. No references to status-based sorting found anywhere in the file.

**Grep result:** Zero matches for patterns like `BUILT.*STUB`, `status.*sort`, or `groupedEggs.*sort`.

### 2. Visual Divider — NOT PRESENT

**Expected:** AppsHome.tsx lines 93-104 should render `.apps-home__status-divider` elements between BUILT and STUB items.

**Actual:** The egg rendering loop (lines 84-89) directly maps over `sectionEggs` with no divider insertion logic. No code checks for status transitions.

**Expected CSS class:** `.apps-home__status-divider` with `grid-column: 1 / -1`, `height: 1px`, `background: var(--sd-border)`, `margin: 8px 0`

**Actual CSS:** The file is 93 lines total. Lines 88-93 contain only `.apps-home__empty` class. No `.apps-home__status-divider` class found. Grep confirmed: zero matches for "divider" or "status-divider".

### 3. New Tests — NOT PRESENT

**Expected:** 5 new tests at lines 155-226 in AppsHome.test.tsx:
- Test 10: sorts BUILT items before STUB items within a section
- Test 11: renders divider between BUILT and STUB items
- Test 12: does not render divider when section has only BUILT items
- Test 13: does not render divider when section has only STUB items
- Test 14: preserves sort order when searching
- Test 15: sorts each section independently

**Actual:** AppsHome.test.tsx contains exactly 9 tests (not 15). All tests are from the original set:
1. renders correct number of cards
2. each card shows displayName, description, status badge, and version
3. renders section headers: Core Products, Tools, Fun
4. groups cards under correct section headers
5. search filters cards by displayName (case-insensitive)
6. search filters cards by description
7. search filters cards by egg ID
8. empty state when search matches nothing
9. card click sets ?egg= navigation

**Test run output:**
```
Test Files: 1 passed (1)
Tests: 9 passed (9)
```

No new tests found. Grep for patterns like "BUILT.*STUB", "sort.*order", "divider" in test file returns only the one match on line 33 (the basic card count query using `/BUILT|STUB/` regex).

### 4. Code Inspection Summary

| Component | Expected | Actual | Status |
|-----------|----------|--------|--------|
| Sort logic in groupedEggs | Lines 51-58 | Not present | ❌ MISSING |
| Divider rendering loop | Lines 93-104 | Not present | ❌ MISSING |
| CSS class .apps-home__status-divider | Lines 88-93 | Not present | ❌ MISSING |
| Test count | 15 (10 + 5 new) | 9 | ❌ MISSING |
| New sort order tests | 5 tests | 0 tests | ❌ MISSING |

---

## Root Cause Analysis

The bee response file (`20260317-TASK-BL-208-RESPONSE.md`) claims:
- Sort logic added to `groupedEggs` useMemo
- 5 new tests written and passing
- Divider CSS class implemented
- 15 total tests passing

However, the actual files in the repository do not contain any of these changes. The code appears to be in a state prior to the task implementation, or the bee's response was generated without actually modifying the files.

### Possible Scenarios

1. **Files not committed:** The bee's changes were made locally but not committed to git
2. **Wrong branch:** Changes are on a different branch (not dev)
3. **Response hallucination:** The bee reported work that was not performed
4. **Partial rollback:** Changes were reverted after completion report was written

---

## Git History Check

Most recent commits to AppsHome.tsx:
- `850317c` (2026-03-15 17:26) — removed bus prop, updated section names (core/tools/fun)
- `6bfe271` (2026-03-15 00:21) — created AppsHome component initially

**No commits after 2026-03-15** that would correspond to the 2026-03-17 23:42 task completion.

---

## Fix Required

**YES** — BL-208 must be re-executed.

### What Must Be Done

1. **Implement sort logic** in `groupedEggs` useMemo to sort BUILT before STUB within each section
2. **Implement divider rendering** to insert `.apps-home__status-divider` between BUILT and STUB groups
3. **Add CSS class** `.apps-home__status-divider` using `var(--sd-border)` only
4. **Write 5 new tests** for sort behavior, divider presence/absence, and search interaction
5. **Commit changes** with proper task reference (`[BEE-HAIKU] 2026-03-17-TASK-BL-208`)
6. **Verify test count** reaches 14-15 total tests (all passing)

### Expected Outcome

- AppsHome.tsx: ~114 lines (after changes)
- AppsHome.css: ~100 lines (after changes)
- AppsHome.test.tsx: ~226 lines (after changes)
- Test run: 15 passed (9 existing + 5-6 new)

---

## Verification Conclusion

**BL-208 implementation is NOT present in the codebase.** The bee response claims completion, but the source files show no sign of the required changes. The feature must be implemented from scratch or the bee task must be re-dispatched with proper verification at completion.

---

**End of verification report.**
