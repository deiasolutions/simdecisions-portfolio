# CANVAS3-EGG: Create canvas3.egg.md -- COMPLETE

**Status:** COMPLETE
**Model:** Opus
**Date:** 2026-03-28

## Files Modified

- `C:\Users\davee\OneDrive\Documents\GitHub\shiftcenter\eggs\canvas3.egg.md` (CREATED, 325 lines)

## What Was Done

- Created `eggs/canvas3.egg.md` with the exact layout specified in the briefing
- Root split: horizontal, ratio `["36px", "30px", "1fr", "24px"]`, 4 children:
  1. `top-bar` (36px) — nodeId: `chrome-top`, seamless, config: appName + showBreadcrumb
  2. `menu-bar` (30px) — nodeId: `chrome-menu`, seamless, empty config
  3. Main content split — vertical, ratio `[0.20, 0.55, 0.25]`, 3 children:
     - Sidebar (palette + properties panels) — nodeId: `canvas-sidebar`, appType: sidebar, hideActivityBar: false, two panels (design + properties)
     - Canvas sim — nodeId: `canvas-editor`, appType: sim, design mode with zoom + grid snap
     - Chat — nodeId: `canvas-chat`, appType: text-pane, renderMode: chat
  4. `status-bar` (24px) — nodeId: `chrome-status`, seamless, currencies: clock/coin/carbon + showConnection
- UI block: chromeMode auto, commandPalette true, akk true — no legacy flags
- Copied all 6 blocks from canvas2.egg.md: commands (6 commands), settings, away, startup, permissions, prompt
- All appTypes verified against APP_REGISTRY in `browser/src/apps/index.ts`: top-bar, menu-bar, status-bar, sidebar, sim, text-pane are all registered

## Test Results

- Test suite: eggToShell (4 test files, 43 tests)
- Result: **43 passed, 0 failed**
- Files run:
  - `src/shell/__tests__/eggToShell.test.ts` (15 tests)
  - `src/shell/__tests__/eggToShell.multiChild.test.ts` (16 tests)
  - `src/shell/__tests__/eggToShell.showChrome.test.ts` (5 tests)
  - `src/shell/__tests__/eggToShell.slideover.test.ts` (7 tests)

## Build Verification

- eggToShell tests all pass (43/43)
- Layout JSON is valid (verified by reading back the file)
- All 4 chrome zones present: top-bar, menu-bar, main split, status-bar
- File is 325 lines (under 500-line max)

## Acceptance Criteria

- [x] Create `eggs/canvas3.egg.md` with exact layout from briefing
- [x] Root split: horizontal, ratio ["36px", "30px", "1fr", "24px"], 4 children
- [x] top-bar (36px) — seamless chrome pane with appName + showBreadcrumb config
- [x] menu-bar (30px) — seamless chrome pane with empty config
- [x] Main content split — vertical, ratio [0.20, 0.55, 0.25], 3 children (sidebar, canvas, chat)
- [x] status-bar (24px) — seamless chrome pane with currencies + showConnection config
- [x] UI block: chromeMode auto, commandPalette true, akk true — no legacy flags
- [x] Commands block copied from canvas2 (6 commands)
- [x] Settings block copied from canvas2
- [x] Away block copied from canvas2
- [x] Startup block copied from canvas2
- [x] Permissions block copied from canvas2
- [x] Prompt block copied from canvas2
- [x] eggToShell tests pass
- [x] File under 500 lines (325 lines)
- [x] No source files modified
- [x] canvas2.egg.md untouched

## Clock / Cost / Carbon

- **Clock:** ~3 minutes
- **Cost:** ~$0.10
- **Carbon:** ~0.02 gCO2e

## Issues / Follow-ups

- None. Clean creation. All tests pass. No issues found.
