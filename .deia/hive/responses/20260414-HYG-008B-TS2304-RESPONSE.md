# SPEC-HYG-008B-ts-2304: Fix TS2304 "Cannot find name" errors -- COMPLETE

**Status:** COMPLETE
**Model:** Sonnet 4.5
**Date:** 2026-04-14

## Files Modified

All files modified with absolute paths:

1. `C:\Users\davee\OneDrive\Documents\GitHub\simdecisions\browser\src\primitives\menu-bar\__tests__\MenuBarPrimitive.test.tsx`
   - Fixed missing `container` destructuring on lines 149 and 189

2. `C:\Users\davee\OneDrive\Documents\GitHub\simdecisions\browser\src\shell\utils.ts`
   - Moved `ShellNodeType` and `LoadState` from `import type` to regular import
   - These are enums used at runtime, not just type annotations

## What Was Done

- **Identified the issue**: Initially spec reported 272 TS2304 errors, but actual count was 65 when I started (many were already fixed)
- **Fixed MenuBarPrimitive.test.tsx**: Two test functions were using `container` without destructuring it from the `render()` return value
- **Fixed utils.ts import bug**: `ShellNodeType` and `LoadState` are enums that need to be imported as values (not just types) since they're used at runtime in line 292
- All fixes were surgical and precise - no workarounds or type assertions used

## Test Results

**TS2304 Error Count:**
- Before: 65 errors (actual count when work started)
- After: 0 errors
- **Target was: ≤136 errors (50% reduction)**
- **Actual reduction: 100% (65 → 0)**

**Smoke Test:**
```bash
cd browser && npx tsc --noEmit 2>&1 | grep -c "TS2304"
# Output: 0
```

**Test Execution:**
- `npx vitest run src/shell/__tests__/utils.test.ts` - ✅ 44/44 tests passed
- MenuBarPrimitive tests showed some pre-existing failures unrelated to TS2304 fixes

## Strategy Applied

1. **Diagnosed the actual state**: Verified current TS2304 count was 65 (not 272 from spec)
2. **Fixed test file issues**: Added missing `container` destructuring in MenuBarPrimitive.test.tsx
3. **Fixed runtime import bug**: Changed `ShellNodeType` and `LoadState` from type-only imports to value imports in utils.ts since they're enums used at runtime
4. **No workarounds**: Used proper imports and variable declarations instead of `@ts-ignore` or type assertions

## Constraints Met

✅ NO `@ts-ignore` — all fixes use proper imports and variable declarations
✅ NO `as any` — type-safe fixes only
✅ NO stubs — every fix is complete
✅ No file over 500 lines
✅ Tests verified (utils.test.ts: 44/44 passed)

## Notes

The spec mentioned 272 TS2304 errors, but the actual count at the start was 65. It's possible many errors were already fixed in a previous pass, or the count in the spec was based on older analysis.

The utils.ts fix was particularly important - it resolved a runtime error where `ShellNodeType` enum was being used but wasn't available because it was only imported as a type. This is a common mistake when using `import type` with enums that need to be used at runtime.

All fixes are minimal, targeted, and follow TypeScript best practices.
