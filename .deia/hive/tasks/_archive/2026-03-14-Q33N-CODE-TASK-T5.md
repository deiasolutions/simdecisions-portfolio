# TASK T5: Tests for AppsHome + EggRegistryService

**Batch:** APPS-HOME | **Wave:** 3 (after T4)
**Assigned to:** BEE (Sonnet)
**Date:** 2026-03-14
**Dependencies:** T2 (AppsHome component), T3 (registry service)

---

## Deliverables

**Files to create:**
- `browser/src/primitives/apps-home/__tests__/AppsHome.test.tsx`
- `browser/src/services/egg-registry/__tests__/eggRegistryService.test.ts`
- `browser/src/services/egg-registry/__tests__/parseEggFrontmatter.test.ts`

---

## Specification

This is the holdout-set QA pass. T2 and T3 bees may have written their own inline tests. T5 is independent validation — write tests from scratch against the acceptance criteria without looking at any inline tests.

### 1. `AppsHome.test.tsx` — component tests

Use `vitest` + `@testing-library/react`. Follow patterns in `browser/src/primitives/kanban-pane/__tests__/KanbanPane.test.tsx` or `browser/src/primitives/terminal/__tests__/TerminalApp.paneNav.test.tsx`.

**Test cases:**

1.1. Renders correct number of cards (14) when given full mock data
1.2. Each card shows displayName, description, status badge, and version
1.3. Section headers render for each group ("Core Products", "Productivity", "Platform Tools")
1.4. Cards are grouped under correct section headers
1.5. Search input filters cards by displayName (case-insensitive)
1.6. Search input filters cards by description (case-insensitive)
1.7. Search input filters cards by egg ID (case-insensitive)
1.8. Empty state shows "No apps match your search" when search matches nothing
1.9. Card click calls `window.location.href` for cards with `subdomain`
1.10. Card click emits `egg:inflate` bus event for cards without `subdomain`
1.11. Cards with BUILT status have correct badge text

**Mock data:** Import `getRegistry()` from `browser/src/services/egg-registry` and use its output as mock data. If T3 isn't wired yet, use a local `MOCK_EGGS` array matching the `EggMeta` interface with at least 5 items across all 3 sections.

**Bus mock:** Create a minimal bus mock with `publish` as a `vi.fn()`:

```typescript
const mockBus = {
  publish: vi.fn(),
  subscribe: vi.fn(() => () => {}),
  unsubscribe: vi.fn(),
}
```

### 2. `eggRegistryService.test.ts` — service tests

**Test cases:**

2.1. `getRegistry()` returns array of length 14
2.2. Every item has all required `EggMeta` fields (egg, displayName, description, version, status, icon, color, section, subdomain)
2.3. No item has `undefined` or empty `egg` field
2.4. Section mapping: `code-default` → `core`, `writers-room` → `productivity`, `design-builder` → `platform`
2.5. Color mapping: `code-default` → `purple`, `chat-default` → `green`
2.6. `getRegistryBySection('core')` returns 6 items
2.7. `getRegistryBySection('productivity')` returns 4 items
2.8. `getRegistryBySection('platform')` returns 4 items
2.9. Icon is first character of displayName for every item
2.10. Subdomain is `string | null` for every item (never `undefined`)

### 3. `parseEggFrontmatter.test.ts` — parser tests

**Test cases:**

3.1. Parses valid `.egg.md` frontmatter with all fields
3.2. Extracts `egg`, `displayName`, `description`, `version` correctly
3.3. Missing `description` defaults to `""`
3.4. Missing `version` defaults to `"0.0.0"`
3.5. Missing `_stub` defaults to `false`
3.6. Missing `displayName` defaults to `egg` value
3.7. Returns `null` for input without frontmatter delimiters
3.8. Returns `null` for empty string
3.9. Handles frontmatter with extra whitespace
3.10. Derives `icon` from first character of `displayName`
3.11. Parses content after frontmatter without affecting extracted values

**Sample frontmatter for tests:**

```typescript
const VALID_EGG = `---
egg: test-app
version: 1.0.0
displayName: Test App
description: A test application
_stub: false
---

# Test App

Body content here.
`;
```

---

## Acceptance Criteria

- [ ] All tests pass (`npx vitest run` from `browser/`)
- [ ] AppsHome tests: at least 10 test cases covering rendering, search, clicks, empty state
- [ ] eggRegistryService tests: at least 8 test cases covering data shape, mappings, filters
- [ ] parseEggFrontmatter tests: at least 8 test cases covering parsing, defaults, edge cases
- [ ] No snapshot tests — explicit assertions only
- [ ] No file exceeds 500 lines
- [ ] No hardcoded colors in test assertions — assert CSS variable usage where applicable

---

## Constraints

1. **No snapshot tests.** All assertions must be explicit (`expect(x).toBe(y)`).
2. **No stubs.** Every test case fully implemented with real assertions.
3. **No file over 500 lines.**
4. **Import from the barrel exports.** Use `from '../../services/egg-registry'` and `from '.'` — not direct file imports.
5. **Follow existing test patterns.** Look at `browser/src/primitives/kanban-pane/__tests__/KanbanPane.test.tsx` for component test patterns.
6. **Do not modify T2 or T3 output files.** Tests are read-only consumers of those modules.

---

## Response Requirements — MANDATORY

When you finish your work, write a response file:
  `.deia/hive/responses/YYYYMMDD-TASK-T5-RESPONSE.md`

The response MUST contain these 8 sections:
1. **Header** — task ID, title, status (COMPLETE/FAILED), model, date
2. **Files Modified** — every file created/modified/deleted, full paths
3. **What Was Done** — bullet list of concrete changes
4. **Test Results** — test files run, pass/fail counts, or why tests weren't written
5. **Build Verification** — vite build output (last 5 lines), vitest run summary
6. **Acceptance Criteria** — copy from task, mark [x] or [ ] with explanation
7. **Clock / Cost / Carbon** — all three, never omit any
8. **Issues / Follow-ups** — edge cases, dependencies, recommended next tasks

DO NOT skip any section. A response without all 8 sections is incomplete.

The response MUST also include YAML frontmatter:

```yaml
---
features_delivered:
  - APPS-HOME-006: AppsHome component test suite
  - APPS-HOME-007: EggRegistryService test suite
  - APPS-HOME-008: parseEggFrontmatter test suite
features_modified: []
features_broken: []
test_summary:
  total: 0
  passed: 0
  failed: 0
---
```
