# REBUILD-TASK-146: Port DES Routes Registration

## Original Task
TASK-146 — Port DES engine routes (`/api/des/run`, `/api/des/validate`, `/api/des/replicate`, `/api/des/status`) to shiftcenter and register in hivenode routes.

## Files to Modify
- `hivenode/routes/__init__.py` — add import and router registration for des_routes

## Exact Edits

### hivenode/routes/__init__.py

**Edit 1: Add import**
```
old_string:
from hivenode.routes import health, auth, ledger_routes, storage_routes, node, llm_routes, shell, sync_routes, kanban_routes, progress_routes, build_monitor, sim, inventory_routes
from hivenode.repo import routes as repo_routes
from hivenode.rag import routes as rag_routes
from hivenode.rag.indexer import routes as indexer_routes
from hivenode.rag.bok import routes as bok_routes
from hivenode.efemera import routes as efemera_routes
from hivenode.entities import routes as entity_routes
from engine.phase_ir import schema_routes as phase_schema_routes
from engine.phase_ir import trace_routes as phase_trace_routes
from engine.phase_ir import validation_routes as phase_validation_routes

new_string:
from hivenode.routes import health, auth, ledger_routes, storage_routes, node, llm_routes, shell, sync_routes, kanban_routes, progress_routes, build_monitor, sim, inventory_routes, des_routes
from hivenode.repo import routes as repo_routes
from hivenode.rag import routes as rag_routes
from hivenode.rag.indexer import routes as indexer_routes
from hivenode.rag.bok import routes as bok_routes
from hivenode.efemera import routes as efemera_routes
from hivenode.entities import routes as entity_routes
from engine.phase_ir import schema_routes as phase_schema_routes
from engine.phase_ir import trace_routes as phase_trace_routes
from engine.phase_ir import validation_routes as phase_validation_routes
```

**Edit 2: Register router**
```
old_string:
    router.include_router(inventory_routes.router, prefix='/api/inventory', tags=['inventory'])
    router.include_router(phase_schema_routes.router, tags=['phase-ir'])
    router.include_router(phase_trace_routes.router, tags=['phase-traces'])
    router.include_router(phase_validation_routes.router, tags=['phase-ir'])
    router.include_router(entity_routes.router, tags=['bot-embeddings'])

    return router

new_string:
    router.include_router(inventory_routes.router, prefix='/api/inventory', tags=['inventory'])
    router.include_router(des_routes.router, tags=['des-engine'])
    router.include_router(phase_schema_routes.router, tags=['phase-ir'])
    router.include_router(phase_trace_routes.router, tags=['phase-traces'])
    router.include_router(phase_validation_routes.router, tags=['phase-ir'])
    router.include_router(entity_routes.router, tags=['bot-embeddings'])

    return router
```

## Dependencies
None — this task registers routes that were already created in TASK-146.

## Sources Used
- `.deia/hive/responses/20260315-1107-BEE-HAIKU-2026-03-15-TASK-146-PORT-DES-ENGINE-ROUTES-RAW.txt`
- `.deia/hive/responses/20260315-TASK-146-RESPONSE.md`
- Current state: `hivenode/routes/__init__.py` and `hivenode/routes/des_routes.py`

## Confidence
HIGH — The edits are clearly visible in current __init__.py (des_routes already registered at line 37), confirming these exact changes were made during TASK-146.
