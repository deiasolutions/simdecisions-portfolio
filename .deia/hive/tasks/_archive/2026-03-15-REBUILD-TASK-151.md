# REBUILD-TASK-151: Port RAG Models + Exports

## Original Task
TASK-151 — Port RAG indexer Pydantic models and update __init__.py exports. Fix CCC_PER_FILE constant in indexer_service.

## Files to Modify
- `hivenode/rag/indexer/__init__.py` — add model exports
- `hivenode/rag/indexer/indexer_service.py` — fix CCC_PER_FILE constant

## Exact Edits

### hivenode/rag/indexer/__init__.py

**Edit 1: Update model exports**
```
old_string:
"""RAG Indexer module.

This module provides data models and file scanning functionality for
the RAG indexing system.
"""

from hivenode.rag.indexer.models import (
    ArtifactType,
    CCCMetadata,
    EmbeddingRecord,
    IndexRecord,
    IRPair,
    IRStatus,
    IRSummary,
    ProvenanceInfo,
    RelevanceMetrics,
    ReliabilityMetrics,
    StalenessInfo,
    StorageTier,
)
from hivenode.rag.indexer.scanner import scan

__all__ = [
    "ArtifactType",
    "StorageTier",
    "IRStatus",
    "CCCMetadata",
    "IRPair",
    "IRSummary",
    "ReliabilityMetrics",
    "RelevanceMetrics",
    "StalenessInfo",
    "ProvenanceInfo",
    "EmbeddingRecord",
    "IndexRecord",
    "scan",
]

new_string:
"""RAG Indexer module.

This module provides data models and file scanning functionality for
the RAG indexing system.
"""

from hivenode.rag.indexer.models import (
    ArtifactType,
    CCCMetadata,
    EmbeddingRecord,
    IndexRecord,
    IRPair,
    IRStatus,
    IRSummary,
    ProvenanceInfo,
    RelevanceMetrics,
    ReliabilityMetrics,
    StalenessInfo,
    StorageTier,
)
from hivenode.rag.indexer.scanner import scan

__all__ = [
    "ArtifactType",
    "StorageTier",
    "IRStatus",
    "CCCMetadata",
    "IRPair",
    "IRSummary",
    "ReliabilityMetrics",
    "RelevanceMetrics",
    "StalenessInfo",
    "ProvenanceInfo",
    "EmbeddingRecord",
    "IndexRecord",
    "scan",
]
```

### hivenode/rag/indexer/indexer_service.py

**Edit 1: Fix CCC_PER_FILE constant**
```
old_string:
# CCC estimation constants
CCC_PER_FILE = CCCMetadata(
    clock_ms=10,        # 10ms per file
    coin_usd=0.0001,    # $0.0001 per file
    carbon_kg=0.000002  # 2 micrograms CO2e per file
)

new_string:
# CCC estimation constants
CCC_PER_FILE = CCCMetadata(
    clock_ms=10,        # 10ms per file
    coin_usd=0.0001,    # $0.0001 per file
    carbon_kg=0.000002  # 2 micrograms CO2e per file
)
```

## Dependencies
None — this task stands alone. TASK-152 depends on this.

## Sources Used
- `.deia/hive/responses/20260315-1417-BEE-HAIKU-2026-03-15-TASK-151-PORT-RAG-MODELS-RAW.txt`
- `.deia/hive/responses/20260315-TASK-151-RESPONSE.md`
- Current state: `hivenode/rag/indexer/__init__.py` and `hivenode/rag/indexer/indexer_service.py`

## Confidence
HIGH — The models.py file exists with all expected models, and __init__.py and indexer_service.py are already in the target state matching the response documentation.
