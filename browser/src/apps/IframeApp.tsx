/**
 * IframeApp
 *
 * * IframeApp.tsx — Generic iframe wrapper component
 *
 * A reusable iframe renderer for embedding HTML content in the shell.
 * Reads config.src for the iframe URL, applies security sandbox settings,
 * and renders a full-size iframe with no border.
 *
 * Config shape:
 * {
 *   src: string;         // URL to load (required)
 *   sandbox?: string;    // Override sandbox attribute (default: "allow-scripts allow-same-origin")
 *   allow?: string;      // Permissions policy (default: "autoplay")
 * }
 *
 * Dependencies:
 * - import type { AppRendererProps } from '../shell/components/appRegistry'
 *
 * Components/Functions:
 * - IframeApp: TypeScript function/component
 * - typedConfig: TypeScript function/component
 * - src: TypeScript function/component
 * - sandbox: TypeScript function/component
 * - allow: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
