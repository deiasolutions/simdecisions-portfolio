/**
 * simAdapter.integration.test
 *
 * * simAdapter.integration.test.tsx — Integration tests for sim adapter
 *
 * Tests full mount of simAdapter, verifies FlowDesigner renders correctly.
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach } from 'vitest'
 * - import { render, screen } from '@testing-library/react'
 * - import { SimAdapter } from '../simAdapter'
 * - import { ShellCtx } from '../../infrastructure/relay_bus'
 * - import type { MessageBus } from '../../infrastructure/relay_bus/messageBus'
 *
 * Components/Functions:
 * - mockBus: TypeScript function/component
 * - mockCtx: TypeScript function/component
 * - canvas: TypeScript function/component
 * - toolbar: TypeScript function/component
 * - palette: TypeScript function/component
 * - ctxNoBus: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
