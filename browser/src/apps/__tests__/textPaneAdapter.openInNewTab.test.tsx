/**
 * textPaneAdapter.openInNewTab.test
 *
 * * textPaneAdapter.openInNewTab.test.tsx — Tests for BUG-046 fix
 *
 * Verifies that TextPaneAdapter passes onOpenInNewTab callback to SDEditor,
 * and that the callback dispatches ADD_TAB with a pre-configured text-pane node.
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach } from 'vitest'
 * - import { render, screen } from '@testing-library/react'
 * - import { TextPaneAdapter } from '../textPaneAdapter'
 * - import { ShellCtx } from '../../infrastructure/relay_bus'
 * - import { ShellNodeType } from '../../shell/types'
 *
 * Components/Functions:
 * - mockDispatch: TypeScript function/component
 * - mockBus: TypeScript function/component
 * - mockCtx: TypeScript function/component
 * - action: TypeScript function/component
 * - id1: TypeScript function/component
 * - id2: TypeScript function/component
 * - noDispatchCtx: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
