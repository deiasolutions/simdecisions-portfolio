/**
 * drawingCanvasAdapter
 *
 * * drawingCanvasAdapter.tsx — App registry adapter for drawing-canvas primitive
 *
 * Maps AppRendererProps to DrawingCanvasApp props.
 * Gets bus from ShellCtx, passes nodeId and config through.
 *
 * Ported from platform. Requires p5 npm dependency.
 *
 * Dependencies:
 * - import { useContext } from 'react'
 * - import { ShellCtx } from '../infrastructure/relay_bus'
 * - import { DrawingCanvasApp } from '../primitives/drawing-canvas/DrawingCanvasApp'
 * - import type { AppRendererProps } from '../shell/components/appRegistry'
 *
 * Components/Functions:
 * - DrawingCanvasAdapter: TypeScript function/component
 * - ctx: TypeScript function/component
 * - bus: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
