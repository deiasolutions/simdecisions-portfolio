/**
 * index
 *
 * * apps/index.ts — App registration entry point
 *
 * Registers all app adapters with the shell's app registry.
 * Called by main.tsx on application startup.
 *
 * Dependencies:
 * - import { registerApp } from '../shell/components/appRegistry'
 * - import { TerminalAdapter } from './terminalAdapter'
 * - import { TextPaneAdapter } from './textPaneAdapter'
 * - import { TreeBrowserAdapter } from './treeBrowserAdapter'
 * - import { DrawingCanvasAdapter } from './drawingCanvasAdapter'
 * - import { CanvasAdapter } from './canvasAdapter'
 * - import { KanbanAdapter } from './kanbanAdapter'
 * - import { ProgressAdapter } from './progressAdapter'
 * - import { BuildMonitorAdapter } from './buildMonitorAdapter'
 * - import { BuildDataService } from './buildDataService'
 *
 * Components/Functions:
 * - registerApps: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
