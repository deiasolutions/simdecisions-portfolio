/**
 * progressAdapter
 *
 * * progressAdapter.tsx — App registry adapter for progress-pane primitive
 *
 * Maps AppRendererProps to ProgressPane props.
 * No special bus subscriptions (read-only for now).
 * Future: subscribe to STAGE_UPDATED events from sync engine.
 *
 * Dependencies:
 * - import { useContext } from 'react';
 * - import { ShellCtx } from '../infrastructure/relay_bus';
 * - import { ProgressPane } from '../primitives/progress-pane/ProgressPane';
 * - import type { AppRendererProps } from '../shell/components/appRegistry';
 *
 * Components/Functions:
 * - ProgressAdapter: TypeScript function/component
 * - ctx: TypeScript function/component
 * - bus: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
