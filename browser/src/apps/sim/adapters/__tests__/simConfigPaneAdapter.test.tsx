/**
 * simConfigPaneAdapter.test
 *
 * * simConfigPaneAdapter.test.tsx — Tests for SimConfigPaneAdapter
 *
 * TDD: Tests written FIRST before implementation.
 * Verifies that SimConfigPanel receives config via MessageBus, publishes changes via bus.
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
 * - import { render, screen, fireEvent, waitFor } from '@testing-library/react'
 * - import { SimConfigPaneAdapter } from '../simConfigPaneAdapter'
 * - import { ShellCtx } from '../../../../infrastructure/relay_bus'
 * - import type { MessageBus } from '../../../../infrastructure/relay_bus/messageBus'
 *
 * Components/Functions:
 * - config: TypeScript function/component
 * - config: TypeScript function/component
 * - replicationsInput: TypeScript function/component
 * - configUpdateMsg: TypeScript function/component
 * - config: TypeScript function/component
 * - startButton: TypeScript function/component
 * - startMsg: TypeScript function/component
 * - config: TypeScript function/component
 * - stopButton: TypeScript function/component
 * - stopMsg: TypeScript function/component
 * - config: TypeScript function/component
 * - replicationsInput: TypeScript function/component
 * - config: TypeScript function/component
 * - mockUnsubscribe: TypeScript function/component
 * - config: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
