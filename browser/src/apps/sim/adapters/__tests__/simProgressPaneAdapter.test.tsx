/**
 * simProgressPaneAdapter.test
 *
 * * simProgressPaneAdapter.test.tsx — Tests for SimProgressPaneAdapter
 *
 * TDD: Tests written FIRST before implementation.
 * Verifies that ProgressPanel + ResultsPreview receive data via MessageBus.
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
 * - import { render, screen, within } from '@testing-library/react'
 * - import { SimProgressPaneAdapter } from '../simProgressPaneAdapter'
 * - import { ShellCtx } from '../../../../infrastructure/relay_bus'
 * - import type { MessageBus } from '../../../../infrastructure/relay_bus/messageBus'
 * - import type { MessageEnvelope } from '../../../../infrastructure/relay_bus/types/messages'
 *
 * Components/Functions:
 * - progressUpdate: TypeScript function/component
 * - metricsUpdate: TypeScript function/component
 * - eventMsg: TypeScript function/component
 * - resultsMsg: TypeScript function/component
 * - mockUnsubscribe: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
