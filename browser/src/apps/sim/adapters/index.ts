/**
 * index
 *
 * * adapters/index.ts — barrel export for the Phase API adapter layer.
 *
 * WB-003: API adapter interface definition.
 * WB-A02: LocalAPIClient added for localhost / Raqcoon server.
 * WB-B01: ApiClientContext added for React context injection.
 *
 * Usage:
 *   import { createClient, CloudAPIClient, LocalAPIClient, type PhaseAPIClient } from "../adapters";
 *   import { ApiClientProvider, useApiClient } from "../adapters";
 *
 * Dependencies:
 * - (see source)
 *
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
