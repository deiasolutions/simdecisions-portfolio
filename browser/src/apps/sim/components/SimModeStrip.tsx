/**
 * SimModeStrip
 *
 * * SimModeStrip — Vertical icon strip for sim mode switching.
 * Replaces the horizontal mode pills from FlowToolbar when running inside ShiftCenter shell.
 * ~40px wide, sits as the leftmost pane in the sim layout.
 *
 * Dependencies:
 * - import { useState, useEffect, useCallback, type ReactElement } from 'react';
 * - import type { MessageBus } from '../../../infrastructure/relay_bus/messageBus';
 * - import type { FlowMode } from './flow-designer/types';
 *
 * Components/Functions:
 * - MODE_BUTTONS: TypeScript function/component
 * - SimModeStrip: TypeScript function/component
 * - unsub: TypeScript function/component
 * - handleClick: TypeScript function/component
 * - isActive: TypeScript function/component
 * - getActiveBg: TypeScript function/component
 * - active: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
