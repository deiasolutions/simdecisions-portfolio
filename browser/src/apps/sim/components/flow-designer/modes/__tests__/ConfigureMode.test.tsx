/**
 * ConfigureMode.test
 *
 * * ConfigureMode.test.tsx — TDD tests for ConfigureMode component
 *
 * ConfigureMode provides:
 * 1. Read-only canvas (no drag, no connect, no edits)
 * 2. Validation panel showing flow issues (errors/warnings)
 * 3. Sim config panel for setting up simulation parameters
 * 4. Mode switching to/from configure mode
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach } from 'vitest'
 * - import { render, screen, waitFor } from '@testing-library/react'
 * - import '@testing-library/jest-dom'
 * - import { ReactFlowProvider } from '@xyflow/react'
 * - import ConfigureMode from '../ConfigureMode'
 * - import type { Node, Edge } from '@xyflow/react'
 *
 * Components/Functions:
 * - actual: TypeScript function/component
 * - validFlow: TypeScript function/component
 * - invalidFlow: TypeScript function/component
 * - flowWithDisconnected: TypeScript function/component
 * - flowWithDecision: TypeScript function/component
 * - reactFlow: TypeScript function/component
 * - onModeChange: TypeScript function/component
 * - exitButton: TypeScript function/component
 * - startButton: TypeScript function/component
 * - startButton: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
