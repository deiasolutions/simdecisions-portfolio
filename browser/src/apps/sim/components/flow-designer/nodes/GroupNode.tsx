/**
 * GroupNode
 *
 * * GroupNode — Sub-process container (dashed bounding box).
 * ReactFlow's "group" / parent node type with a labeled header.
 * Auto-creates Start + End child nodes when dropped empty on canvas.
 *
 * Dragging: the dashed border and label pill are grabbable. The interior
 * passes pointer events through so child nodes remain clickable.
 *
 * Dependencies:
 * - import { memo } from "react";
 * - import { Handle, Position, NodeProps, NodeResizer } from "@xyflow/react";
 * - import { colors, fonts } from "../../../lib/theme";
 * - import type { GroupNodeData } from "../types";
 *
 * Components/Functions:
 * - GroupNode: TypeScript function/component
 * - nodeData: TypeScript function/component
 * - accent: TypeScript function/component
 * - childCount: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
