/**
 * terminalAdapter
 *
 * * terminalAdapter.tsx — App registry adapter for terminal primitive
 *
 * Maps AppRendererProps to TerminalApp props.
 * Reads API key from settings store, manages SettingsModal, passes bus from ShellCtx.
 *
 * Dependencies:
 * - import { useContext, useState, useCallback } from 'react'
 * - import { ShellCtx } from '../infrastructure/relay_bus'
 * - import { TerminalApp } from '../primitives/terminal/TerminalApp'
 * - import { SettingsModal } from '../primitives/settings/SettingsModal'
 * - import { getApiKey } from '../primitives/settings/settingsStore'
 * - import { useIdentity } from '../services/identity'
 * - import type { AppRendererProps } from '../shell/components/appRegistry'
 * - import type { TerminalEggConfig } from '../primitives/terminal/types'
 *
 * Components/Functions:
 * - TerminalAdapter: TypeScript function/component
 * - ctx: TypeScript function/component
 * - bus: TypeScript function/component
 * - user: TypeScript function/component
 * - llmProvider: TypeScript function/component
 * - statusBarCurrencies: TypeScript function/component
 * - promptPrefix: TypeScript function/component
 * - zone2Position: TypeScript function/component
 * - zone2Default: TypeScript function/component
 * - routeTarget: TypeScript function/component
 * - links: TypeScript function/component
 * - diffInput: TypeScript function/component
 * - hideStatusBar: TypeScript function/component
 * - statusBarPosition: TypeScript function/component
 * - brandName: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
