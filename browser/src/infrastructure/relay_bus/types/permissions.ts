/**
 * permissions
 *
 * * Permissions type definitions for EGG-based governance system.
 * Implements trust tier defaults, EGG-level ceilings, and node-level tightening.
 * Ported from simdecisions-2/src/types/permissions.ts
 *
 * Architecture:
 * - EGG permissions = ceiling (what's allowed)
 * - Node permissions = tightening (subset of EGG)
 * - Trust tier = default baseline (platform vs GC vs external)
 *
 * Dependencies:
 * - (see source)
 *
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
