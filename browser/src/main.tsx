/**
 * main
 *
 * * main.tsx — Application entry point
 *
 * 1. Registers app adapters (terminal, text-pane)
 * 2. Imports global CSS (shell themes)
 * 3. Renders App into #root
 *
 * Dependencies:
 * - import { StrictMode } from 'react'
 * - import { createRoot } from 'react-dom/client'
 * - import { App } from './App'
 * - import { registerApps } from './apps'
 * - import './shell/shell-themes.css'
 * - import './shell/cloud-theme.css'
 *
 * Components/Functions:
 * - root: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
