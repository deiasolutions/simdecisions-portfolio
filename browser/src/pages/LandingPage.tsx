/**
 * LandingPage
 *
 * * LandingPage.tsx — Landing page for ShiftCenter
 *
 * Displayed when user visits root URL (/) without an EGG parameter.
 * Shows hero + the AppsHome EGG chooser grid + footer.
 * Reuses the existing AppsHomeAdapter which fetches egg-manifest.json.
 *
 * Rule 3: NO HARDCODED COLORS. Only CSS variables (var(--sd-*)).
 * Rule 4: File under 500 lines.
 *
 * Dependencies:
 * - import { AppsHomeAdapter } from '../apps/appsHomeAdapter'
 * - import './LandingPage.css'
 *
 * Components/Functions:
 * - LandingPage: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
