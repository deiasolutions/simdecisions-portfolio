/**
 * BottomNav.test
 *
 * * __tests__/BottomNav.test.tsx — TDD tests for bottom-nav primitive
 * Tests written FIRST before implementation.
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach } from 'vitest';
 * - import { render, screen } from '@testing-library/react';
 * - import { userEvent } from '@testing-library/user-event';
 * - import { BottomNav } from '../BottomNav';
 * - import { ShellCtx, MessageBus } from '../../../infrastructure/relay_bus';
 * - import type { AppNode, BranchesRoot } from '../../../shell/types';
 * - import type { ShellAction } from '../../../shell/types';
 * - import { ShellNodeType, LoadState } from '../../../shell/types';
 *
 * Components/Functions:
 * - mockDispatch: TypeScript function/component
 * - mockBus: TypeScript function/component
 * - makeAppNode: TypeScript function/component
 * - makeRoot: TypeScript function/component
 * - makeShellState: TypeScript function/component
 * - split: TypeScript function/component
 * - root: TypeScript function/component
 * - state: TypeScript function/component
 * - buttons: TypeScript function/component
 * - split: TypeScript function/component
 * - root: TypeScript function/component
 * - state: TypeScript function/component
 * - buttons: TypeScript function/component
 * - split: TypeScript function/component
 * - root: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
