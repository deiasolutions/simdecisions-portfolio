/**
 * canvas-ir-deposit.test
 *
 * * canvas-ir-deposit.test.tsx — Tests for CanvasApp IR deposit handling
 *
 * Tests terminal → IR → canvas pipeline with various IR formats.
 * Verifies CanvasApp receives terminal:ir-deposit events and renders nodes.
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 * - import { render, screen, waitFor } from '@testing-library/react';
 * - import { CanvasApp } from '../CanvasApp';
 * - import { MessageBus } from '../../../infrastructure/relay_bus/messageBus';
 * - import type { Flow } from '../../../types/ir';
 *
 * Components/Functions:
 * - flow: TypeScript function/component
 * - flow: TypeScript function/component
 * - flow: TypeScript function/component
 * - flow: TypeScript function/component
 * - flow: TypeScript function/component
 * - flow: TypeScript function/component
 * - flow: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
