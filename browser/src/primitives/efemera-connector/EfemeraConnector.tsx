/**
 * EfemeraConnector
 *
 * * EfemeraConnector.tsx — Efemera Connector primitive
 * Supports three view modes via config.view:
 *   'channels' — channels-only pane (left sidebar)
 *   'members'  — members-only pane (right sidebar)
 *   'both'     — tabbed sidebar (backward compat, default)
 * Registered as appType "efemera-connector".
 *
 * Dependencies:
 * - import { useState, useCallback } from 'react'
 * - import { TreeBrowser } from '../tree-browser'
 * - import type { TreeNodeData } from '../tree-browser'
 * - import { groupChannels } from '../tree-browser/adapters/channelsAdapter'
 * - import { groupMembers } from '../tree-browser/adapters/membersAdapter'
 * - import { useEfemeraConnector } from './useEfemeraConnector'
 * - import { useEfemeraMembersPane } from './useEfemeraMembersPane'
 * - import type { AppRendererProps } from '../../shell/components/appRegistry'
 * - import './efemera-connector.css'
 *
 * Components/Functions:
 * - ConnectorError: TypeScript function/component
 * - ConnectionBadge: TypeScript function/component
 * - PaneHeader: TypeScript function/component
 * - EfemeraChannelsView: TypeScript function/component
 * - channelNodes: TypeScript function/component
 * - handleChannelSelect: TypeScript function/component
 * - EfemeraMembersView: TypeScript function/component
 * - memberNodes: TypeScript function/component
 * - handleMemberSelect: TypeScript function/component
 * - EfemeraTabbedView: TypeScript function/component
 * - tabs: TypeScript function/component
 * - defaultTab: TypeScript function/component
 * - channelNodes: TypeScript function/component
 * - memberNodes: TypeScript function/component
 * - handleChannelSelect: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
