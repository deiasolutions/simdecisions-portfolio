/**
 * wsTransport.guard.test
 *
 * * wsTransport.guard.test.ts — Tests for isWebSocketSupported guard function
 * Isolated test file to avoid mock conflicts with useEfemeraConnector tests
 *
 * Dependencies:
 * - import { describe, it, expect, beforeEach, afterEach } from 'vitest'
 * - import { isWebSocketSupported } from '../wsTransport'
 *
 * Components/Functions:
 * - checks: TypeScript function/component
 * - result: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
