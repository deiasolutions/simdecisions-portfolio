/**
 * TabBarPrimitive.test
 *
 * * TabBarPrimitive.test.tsx — TDD tests for tab-bar chrome primitive
 * Tests tab rendering, SWAP_APP dispatch via bus, pinned tabs, allowedTabs menu, scrolling
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach } from 'vitest';
 * - import { render, screen, fireEvent } from '@testing-library/react';
 * - import { TabBarPrimitive } from '../TabBarPrimitive';
 * - import { ShellCtx, MessageBus } from '../../../infrastructure/relay_bus';
 * - import type { ShellState, ShellAction } from '../../../shell/types';
 * - import { ShellNodeType, LoadState } from '../../../shell/types';
 * - import { INITIAL_STATE } from '../../../shell/reducer';
 *
 * Components/Functions:
 * - to: TypeScript class
 * - to: TypeScript class
 * - createMockShellState: TypeScript function/component
 * - createMockShellContext: TypeScript function/component
 * - bus: TypeScript function/component
 * - config: TypeScript function/component
 * - config: TypeScript function/component
 * - config: TypeScript function/component
 * - busEmitSpy: TypeScript function/component
 * - branchTab: TypeScript function/component
 * - config: TypeScript function/component
 * - busEmitSpy: TypeScript function/component
 * - canvasTab: TypeScript function/component
 * - config: TypeScript function/component
 * - closeButton: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
