/**
 * irExtractor
 *
 * * irExtractor.ts — Extract IR JSON blocks from LLM response text.
 *
 * Splits a mixed LLM response into:
 * - chatText: conversational parts ("Does this look correct?")
 * - irBlocks: parsed JSON objects from ```json fenced blocks
 *
 * Used by the 'ir' routeTarget to split responses for canvas + text-pane routing.
 *
 * Dependencies:
 * - (see source)
 *
 * Components/Functions:
 * - extractIRBlocks: TypeScript function/component
 * - irBlocks: TypeScript function/component
 * - jsonBlockPattern: TypeScript function/component
 * - blocksToRemove: TypeScript function/component
 * - jsonStr: TypeScript function/component
 * - parsed: TypeScript function/component
 * - block: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
