/**
 * terminalModes
 *
 * * Terminal Modes — Configuration presets for different terminal contexts
 *
 * These modes define system prompts, tool manifests, and routing configurations
 * for different use cases. Apps can reference these when configuring terminals.
 *
 * Dependencies:
 * - import { FACTORY_TOOLS } from '../../services/frank/toolManifests/factory';
 * - import type { TerminalEggConfig } from './types';
 *
 * Components/Functions:
 * - TERMINAL_MODES: TypeScript function/component
 * - getTerminalMode: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
