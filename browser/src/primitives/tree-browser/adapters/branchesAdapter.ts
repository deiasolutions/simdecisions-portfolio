/**
 * branchesAdapter
 *
 * * branchesAdapter.ts — Branches Adapter for Tree Browser
 * Displays flow branches and their nodes in the sidebar.
 *
 * Listens for: sim:branches-updated
 * Publishes: sim:branch-select (when user clicks a branch)
 *
 * Sections:
 * - Each branch becomes a group node
 * - Branch nodes become leaf children
 *
 * Dependencies:
 * - import type { TreeNodeData } from '../types'
 * - import type { MessageBus } from '../../../infrastructure/relay_bus/messageBus'
 *
 * Components/Functions:
 * - createEmptyState: TypeScript function/component
 * - NODE_TYPE_ICONS: TypeScript function/component
 * - branchesToTreeNodes: TypeScript function/component
 * - createBranchesAdapter: TypeScript function/component
 * - data: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
