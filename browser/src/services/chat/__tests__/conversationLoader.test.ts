/**
 * conversationLoader.test
 *
 * * conversationLoader.test.ts — Tests for conversation loader hook
 * TDD: tests first, implementation follows
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach, waitFor } from 'vitest';
 * - import { renderHook, act } from '@testing-library/react';
 * - import { useConversationLoader } from '../conversationLoader';
 * - import type { MessageBus } from '../../../infrastructure/relay_bus';
 * - import type { ConversationWithMessages } from '../../terminal/types';
 * - import * as chatApiModule from '../../terminal/chatApi';
 * - import * as chatMarkdownModule from '../../terminal/chatMarkdown';
 *
 * Components/Functions:
 * - mockConversation: TypeScript function/component
 * - event: TypeScript function/component
 * - sendCall: TypeScript function/component
 * - event: TypeScript function/component
 * - sendCall: TypeScript function/component
 * - event: TypeScript function/component
 * - sendCall: TypeScript function/component
 * - event: TypeScript function/component
 * - event: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
