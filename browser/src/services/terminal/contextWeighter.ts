/**
 * contextWeighter
 *
 * * ContextWeighter — context-aware weighting for TF-IDF command suggestions
 *
 * Combines TF-IDF scores with runtime context signals to produce more relevant
 * command suggestions based on:
 * - Active pane type
 * - Current directory (git repository detection)
 * - Recent command history
 *
 * Dependencies:
 * - (see source)
 *
 * Components/Functions:
 * - ContextWeighter: TypeScript class
 * - firstToken: TypeScript function/component
 * - firstToken: TypeScript function/component
 * - weighted: TypeScript function/component
 * - weight: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
