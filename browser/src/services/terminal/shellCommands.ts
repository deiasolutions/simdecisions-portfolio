/**
 * shellCommands
 *
 * * shellCommands.ts — Known shell command detection
 *
 * Static list of shell commands used for smart parsing.
 * If first token (lowercased) matches, input is treated as shell command.
 *
 * Dependencies:
 * - (see source)
 *
 * Components/Functions:
 * - SHELL_COMMANDS: TypeScript function/component
 * - isShellCommand: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
