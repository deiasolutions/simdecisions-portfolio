/**
 * shellParser
 *
 * * shellParser.ts — Smart input parsing for terminal
 *
 * Parses user input according to 7 parsing rules:
 * 1. Starts with `!` → force shell
 * 2. Starts with `/` → slash command
 * 3. Starts with `//` → IPC
 * 4. Starts with `>` → palette
 * 5. First token matches known shell command → shell
 * 6. Natural language → chat
 * 7. Ambiguous → chat (safe default)
 *
 * Respects mode (shell/chat/hybrid) and allowShell flag.
 *
 * Dependencies:
 * - import { isShellCommand } from './shellCommands';
 *
 * Components/Functions:
 * - parseInput: TypeScript function/component
 * - trimmed: TypeScript function/component
 * - effectiveMode: TypeScript function/component
 * - withoutBang: TypeScript function/component
 * - parts: TypeScript function/component
 * - command: TypeScript function/component
 * - args: TypeScript function/component
 * - parts: TypeScript function/component
 * - command: TypeScript function/component
 * - args: TypeScript function/component
 * - parts: TypeScript function/component
 * - firstToken: TypeScript function/component
 * - command: TypeScript function/component
 * - args: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
