/**
 * AuthGate
 *
 * * AuthGate.tsx — Branded authentication gate
 *
 * Shown when a set has auth: required and the user is unauthenticated.
 * Uses the set's own identity (favicon, displayName, description) for branding.
 * GitHub OAuth button + conditional dev-login button.
 *
 * Dependencies:
 * - import { useState, useEffect } from 'react'
 * - import { setToken, setUser, setRefreshToken, base64UrlDecode } from '../primitives/auth/authStore'
 * - import { discoverHivenodeUrl } from '../services/hivenodeDiscovery'
 * - import { claimDeviceData } from '../primitives/auth/authStore'
 * - import type { EggIR } from '../sets/types'
 * - import './AuthGate.css'
 *
 * Components/Functions:
 * - API_BASE: TypeScript function/component
 * - RETURN_TO_KEY: TypeScript function/component
 * - GITHUB_ICON: TypeScript function/component
 * - AuthGate: TypeScript function/component
 * - handleAuthSuccess: TypeScript function/component
 * - parts: TypeScript function/component
 * - payload: TypeScript function/component
 * - returnTo: TypeScript function/component
 * - handleGitHubLogin: TypeScript function/component
 * - origin: TypeScript function/component
 * - handleDevLogin: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
