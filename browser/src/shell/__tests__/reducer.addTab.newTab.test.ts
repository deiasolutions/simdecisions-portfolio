/**
 * reducer.addTab.newTab.test
 *
 * * reducer.addTab.newTab.test.ts — Tests for ADD_TAB with pre-configured newTab node
 *
 * BUG-046: When ADD_TAB is dispatched with a newTab AppNode, the new tab
 * should use that node instead of creating an empty pane.
 *
 * Dependencies:
 * - import { describe, it, expect, beforeEach, vi } from 'vitest';
 * - import { shellReducer, INITIAL_STATE } from '../reducer';
 * - import { makeEmpty, uid } from '../utils';
 * - import type { ShellState, AppNode, TabbedNode } from '../types';
 * - import { ShellNodeType, LoadState } from '../types';
 *
 * Components/Functions:
 * - actual: TypeScript function/component
 * - makeTextPaneTab: TypeScript function/component
 * - paneId: TypeScript function/component
 * - newTab: TypeScript function/component
 * - result: TypeScript function/component
 * - layout: TypeScript function/component
 * - secondTab: TypeScript function/component
 * - paneId: TypeScript function/component
 * - resultLayout: TypeScript function/component
 * - tabbedId: TypeScript function/component
 * - newTab: TypeScript function/component
 * - layout: TypeScript function/component
 * - secondTab: TypeScript function/component
 * - paneId: TypeScript function/component
 * - resultLayout: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
