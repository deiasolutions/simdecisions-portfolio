/**
 * turtleDrawEgg.test
 *
 * * turtleDrawEgg.test.ts — Tests for turtle-draw EGG parsing and app type resolution
 *
 * Verifies that turtle-draw.egg.md parses into correct shell state with:
 * - 2 panes: drawing-canvas (left 65%) + terminal (right 35%)
 * - Terminal uses proper config (promptPrefix, zone2Position, zone2Default)
 * - No unregistered app types (no "frank-cli")
 *
 * Dependencies:
 * - import { describe, it, expect } from 'vitest'
 * - import { eggLayoutToShellTree } from '../eggToShell'
 * - import { ShellNodeType } from '../types'
 * - import type { EggLayoutNode } from '../../sets/types'
 *
 * Components/Functions:
 * - buildTurtleDrawLayout: TypeScript function/component
 * - layout: TypeScript function/component
 * - layout: TypeScript function/component
 * - leftPane: TypeScript function/component
 * - layout: TypeScript function/component
 * - rightPane: TypeScript function/component
 * - layout: TypeScript function/component
 * - rightPane: TypeScript function/component
 * - layout: TypeScript function/component
 * - shellTree: TypeScript function/component
 * - layout: TypeScript function/component
 * - shellTree: TypeScript function/component
 * - leftChild: TypeScript function/component
 * - rightChild: TypeScript function/component
 * - layout: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
