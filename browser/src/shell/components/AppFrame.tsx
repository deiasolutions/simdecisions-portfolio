/**
 * AppFrame
 *
 * * AppFrame.tsx — App component loader via registry pattern.
 * Routes appType to the registered renderer from appRegistry.
 * Platform bundled apps register themselves via registerApp().
 *
 * Dependencies:
 * - import type React from 'react';
 * - import { useMemo } from 'react';
 * - import type { AppNode } from '../types';
 * - import { getAppRenderer } from './appRegistry';
 *
 * Components/Functions:
 * - AppFrame: TypeScript function/component
 * - Renderer: TypeScript function/component
 * - stableConfig: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
