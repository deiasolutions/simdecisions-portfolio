/**
 * ClosePromptDialog
 *
 * * ClosePromptDialog.tsx — On-close prompt when dirty flags are true
 * Save / Don't Save / Cancel
 * Portals to .hhp-root, uses CSS variables for styling
 *
 * Dependencies:
 * - import { createPortal } from 'react-dom';
 * - import type React from 'react';
 *
 * Components/Functions:
 * - ClosePromptDialog: TypeScript function/component
 * - dialog: TypeScript function/component
 * - container: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
