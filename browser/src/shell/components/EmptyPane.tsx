/**
 * EmptyPane
 *
 * * EmptyPane.tsx — Empty pane with FAB (Floating Action Button) and right-click fallback.
 * The FAB replaces the old "right-click to add content" text with a visible + button.
 *
 * Dependencies:
 * - import { useState, useMemo, useRef } from 'react';
 * - import { createPortal } from 'react-dom';
 * - import type React from 'react';
 * - import { useShell } from '../../infrastructure/relay_bus';
 * - import { findParent } from '../utils';
 * - import { ContextMenu } from './ContextMenu';
 * - import type { ContextMenuItem } from './ContextMenu';
 * - import { listRegisteredApps } from './appRegistry';
 * - import { APP_REGISTRY } from '../constants';
 * - import type { AppCategory } from '../constants';
 *
 * Components/Functions:
 * - EmptyPane: TypeScript function/component
 * - shell: TypeScript function/component
 * - dispatch: TypeScript function/component
 * - focusedPaneId: TypeScript function/component
 * - root: TypeScript function/component
 * - fabRef: TypeScript function/component
 * - isFocused: TypeScript function/component
 * - parent: TypeScript function/component
 * - canMerge: TypeScript function/component
 * - registeredApps: TypeScript function/component
 * - ALIAS_TYPES: TypeScript function/component
 * - isMultiPane: TypeScript function/component
 * - itemsByCategory: TypeScript function/component
 * - confirmed: TypeScript function/component
 * - url: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
