/**
 * GestureLayer
 *
 * * GestureLayer.tsx — Mobile gesture handling layer
 * Handles: swipe navigation, long-press context menu, double-tap maximize, pinch delegation
 * Edge swipe avoidance (30px inset), overscroll-behavior enforcement
 *
 * Dependencies:
 * - import { useState, useRef, useEffect, useCallback, type ReactNode } from 'react';
 * - import type React from 'react';
 * - import { useShell } from '../../infrastructure/relay_bus';
 * - import { BottomSheet } from './BottomSheet';
 * - import type { ShellTreeNode, AppNode } from '../types';
 *
 * Components/Functions:
 * - collectAppNodes: TypeScript function/component
 * - result: TypeScript function/component
 * - splitNode: TypeScript function/component
 * - tripleNode: TypeScript function/component
 * - tabbedNode: TypeScript function/component
 * - GestureLayer: TypeScript function/component
 * - shell: TypeScript function/component
 * - dispatch: TypeScript function/component
 * - chromeMode: TypeScript function/component
 * - focusedPaneId: TypeScript function/component
 * - root: TypeScript function/component
 * - containerRef: TypeScript function/component
 * - longPressTimer: TypeScript function/component
 * - allPanes: TypeScript function/component
 * - clearLongPressTimer: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
