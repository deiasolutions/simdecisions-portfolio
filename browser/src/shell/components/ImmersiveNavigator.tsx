/**
 * ImmersiveNavigator
 *
 * * ImmersiveNavigator.tsx — Single-pane stack navigator for immersive mode
 * Renders only the focused pane at full viewport size.
 * Preserves split tree in state for restoration when viewport widens.
 * Supports swipe left/right navigation between panes.
 *
 * Dependencies:
 * - import { useEffect, useRef, useState } from 'react';
 * - import type { ReactElement } from 'react';
 * - import { useShell } from '../../infrastructure/relay_bus';
 * - import { ShellNodeRenderer } from './ShellNodeRenderer';
 * - import type { ShellTreeNode, AppNode } from '../types';
 *
 * Components/Functions:
 * - collectAppNodes: TypeScript function/component
 * - result: TypeScript function/component
 * - splitNode: TypeScript function/component
 * - tripleNode: TypeScript function/component
 * - tabbedNode: TypeScript function/component
 * - ImmersiveNavigator: TypeScript function/component
 * - shell: TypeScript function/component
 * - dispatch: TypeScript function/component
 * - bus: TypeScript function/component
 * - containerRef: TypeScript function/component
 * - allPanes: TypeScript function/component
 * - focusedPane: TypeScript function/component
 * - container: TypeScript function/component
 * - handleTouchStart: TypeScript function/component
 * - touch: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
