/**
 * MaximizedOverlay
 *
 * * MaximizedOverlay.tsx — Full-screen overlay for maximized pane.
 * Renders the maximized pane in a full-screen overlay (z-index 200) when a pane is maximized.
 * Restore is handled in PaneChrome chrome bar when the pane is maximized, avoiding overlap with ✕ button.
 *
 * Dependencies:
 * - import type React from 'react';
 * - import { useShell } from '../../infrastructure/relay_bus';
 * - import { ShellNodeRenderer } from './ShellNodeRenderer';
 * - import { findNode } from '../utils';
 *
 * Components/Functions:
 * - MaximizedOverlay: TypeScript function/component
 * - shell: TypeScript function/component
 * - node: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
