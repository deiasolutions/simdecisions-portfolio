/**
 * PaneErrorBoundary
 *
 * * PaneErrorBoundary.tsx — React error boundary for pane isolation
 * Wraps each pane content so one crashing app doesn't take down the whole shell.
 * Uses class component for getDerivedStateFromError and componentDidCatch lifecycle methods.
 *
 * Dependencies:
 * - import type React from 'react';
 * - import { Component, type ErrorInfo, type ReactNode } from 'react';
 * - import { classifyError, type ErrorType } from '../../primitives/terminal/errorClassifier';
 * - import { getErrorMessage } from '../../primitives/terminal/errorMessages';
 *
 * Components/Functions:
 * - component: TypeScript class
 * - PaneErrorBoundary: TypeScript class
 * - errorType: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
