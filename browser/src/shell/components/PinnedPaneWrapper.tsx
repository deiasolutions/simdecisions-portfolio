/**
 * PinnedPaneWrapper
 *
 * * PinnedPaneWrapper.tsx — Fixed-position pinned pane (layout-immune)
 *
 * Dependencies:
 * - import type React from 'react';
 * - import { useShell } from '../../infrastructure/relay_bus';
 * - import { PaneChrome } from './PaneChrome';
 * - import { EmptyPane } from './EmptyPane';
 * - import { AppFrame } from './AppFrame';
 * - import type { AppNode } from '../types';
 *
 * Components/Functions:
 * - PinnedPaneWrapper: TypeScript function/component
 * - shell: TypeScript function/component
 * - dispatch: TypeScript function/component
 * - pos: TypeScript function/component
 * - size: TypeScript function/component
 * - onMouseDown: TypeScript function/component
 * - isOccupied: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
