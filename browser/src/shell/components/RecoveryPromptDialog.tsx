/**
 * RecoveryPromptDialog
 *
 * * RecoveryPromptDialog.tsx — On-return recovery prompt when temp files exist
 * Restore / Discard
 * Portals to .hhp-root, uses CSS variables for styling
 *
 * Dependencies:
 * - import { createPortal } from 'react-dom';
 * - import type React from 'react';
 *
 * Components/Functions:
 * - RecoveryPromptDialog: TypeScript function/component
 * - formatDate: TypeScript function/component
 * - date: TypeScript function/component
 * - formattedDate: TypeScript function/component
 * - dialog: TypeScript function/component
 * - container: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
