/**
 * SplitContainer
 *
 * * SplitContainer.tsx — Binary split wrapper using SplitDivider.
 * Renders two children with a draggable divider between them.
 *
 * Dependencies:
 * - import { useRef } from 'react';
 * - import type React from 'react';
 * - import type { SplitNode } from '../types';
 * - import { ShellNodeRenderer } from './ShellNodeRenderer';
 * - import { SplitDivider } from './SplitDivider';
 *
 * Components/Functions:
 * - SplitContainer: TypeScript function/component
 * - containerRef: TypeScript function/component
 * - isVert: TypeScript function/component
 * - autoSecond: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
