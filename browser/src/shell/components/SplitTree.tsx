/**
 * SplitTree
 *
 * * SplitTree.tsx — Recursive split renderer with react-resizable-panels,
 * context menu, and max depth enforcement.
 *
 * Dependencies:
 * - import React, { useState } from 'react';
 * - import { Panel, PanelGroup, PanelResizeHandle } from 'react-resizable-panels';
 * - import type { SplitNode as SplitNodeType, ShellTreeNode } from '../types';
 * - import { MAX_SPLIT_DEPTH } from '../constants';
 * - import { ContextMenu, type ContextMenuItem } from './ContextMenu';
 * - import './shell.css';
 *
 * Components/Functions:
 * - MAX_DEPTH: TypeScript function/component
 * - SplitTree: TypeScript function/component
 * - isActive: TypeScript function/component
 * - handleContextMenu: TypeScript function/component
 * - handleCloseMenu: TypeScript function/component
 * - menuItems: TypeScript function/component
 * - canSplit: TypeScript function/component
 * - direction: TypeScript function/component
 * - leftSize: TypeScript function/component
 * - rightSize: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
