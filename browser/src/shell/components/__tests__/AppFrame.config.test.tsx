/**
 * AppFrame.config.test
 *
 * * AppFrame.config.test.tsx — Tests for AppFrame config memoization
 * Verifies that config object is referentially stable across re-renders
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach } from 'vitest'
 * - import { render } from '@testing-library/react'
 * - import { AppFrame } from '../AppFrame'
 * - import { LoadState, ShellNodeType } from '../../types'
 * - import type { AppNode } from '../../types'
 * - import * as appRegistry from '../appRegistry'
 *
 * Components/Functions:
 * - createTestNode: TypeScript function/component
 * - receivedConfigs: TypeScript function/component
 * - MockRenderer: TypeScript function/component
 * - node1: TypeScript function/component
 * - node2: TypeScript function/component
 * - node3: TypeScript function/component
 * - receivedConfigs: TypeScript function/component
 * - MockRenderer: TypeScript function/component
 * - config1: TypeScript function/component
 * - node1: TypeScript function/component
 * - node2: TypeScript function/component
 * - node3: TypeScript function/component
 * - receivedConfigs: TypeScript function/component
 * - MockRenderer: TypeScript function/component
 * - config1: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
