/**
 * GestureLayer.test
 *
 * * GestureLayer.test.tsx — TDD tests for mobile gesture handling
 * Tests swipe navigation, long-press context menu, pinch delegation, edge avoidance
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 * - import { render, screen, fireEvent, waitFor, act } from '@testing-library/react';
 * - import { GestureLayer } from '../GestureLayer';
 * - import { ShellCtx } from '../../../infrastructure/relay_bus';
 * - import type { AppNode } from '../../types';
 * - import { ShellNodeType, LoadState } from '../../types';
 *
 * Components/Functions:
 * - mockDispatch: TypeScript function/component
 * - mockBus: TypeScript function/component
 * - mockShell: TypeScript function/component
 * - renderGestureLayer: TypeScript function/component
 * - layer: TypeScript function/component
 * - multiPaneShell: TypeScript function/component
 * - layer: TypeScript function/component
 * - multiPaneShell: TypeScript function/component
 * - layer: TypeScript function/component
 * - layer: TypeScript function/component
 * - layer: TypeScript function/component
 * - layer: TypeScript function/component
 * - layer: TypeScript function/component
 * - layer: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
