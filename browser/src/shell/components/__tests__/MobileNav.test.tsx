/**
 * MobileNav.test
 *
 * * MobileNav.test.tsx — Tests for shell-level mobile bottom navigation
 *
 * Dependencies:
 * - import { describe, it, expect, vi } from 'vitest';
 * - import { render, screen, fireEvent } from '@testing-library/react';
 * - import { ShellCtx } from '../../../infrastructure/relay_bus';
 * - import { MobileNav } from '../MobileNav';
 * - import type { AppNode, SplitNode, BranchesRoot } from '../../types';
 * - import { uid } from '../../../infrastructure/relay_bus';
 * - import { ShellNodeType, LoadState } from '../../types';
 *
 * Components/Functions:
 * - makeApp: TypeScript function/component
 * - makeSplit: TypeScript function/component
 * - makeRoot: TypeScript function/component
 * - renderWithShell: TypeScript function/component
 * - dispatch: TypeScript function/component
 * - bus: TypeScript function/component
 * - root: TypeScript function/component
 * - ctx: TypeScript function/component
 * - result: TypeScript function/component
 * - p1: TypeScript function/component
 * - p2: TypeScript function/component
 * - layout: TypeScript function/component
 * - p1: TypeScript function/component
 * - p2: TypeScript function/component
 * - layout: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
