/**
 * Shell.compact.test
 *
 * * Shell.compact.test.tsx — Tests for compact mode behavior
 * TDD: Tests written FIRST, then implementation
 *
 * Compact mode (600-1024px viewport):
 * - top-bar renders at 28px height
 * - menu-bar returns null (hidden)
 * - menu syndicated items route to command palette
 * - PaneChrome controls collapse to icon-only mode
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach } from 'vitest';
 * - import { render, screen } from '@testing-library/react';
 * - import type { ShellState } from '../../types';
 * - import { INITIAL_STATE } from '../../reducer';
 * - import { TopBar } from '../../../primitives/top-bar/TopBar';
 * - import { MenuBarPrimitive } from '../../../primitives/menu-bar/MenuBarPrimitive';
 * - import { ShellCtx } from '../../../infrastructure/relay_bus';
 * - import { MessageBus } from '../../../infrastructure/relay_bus/messageBus';
 *
 * Components/Functions:
 * - or: TypeScript class
 * - topBar: TypeScript function/component
 * - topBar: TypeScript function/component
 * - topBar: TypeScript function/component
 * - menuBar: TypeScript function/component
 * - menuBar: TypeScript function/component
 * - menuBar: TypeScript function/component
 * - menuBar: TypeScript function/component
 * - menuBar: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
