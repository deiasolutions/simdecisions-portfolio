/**
 * Shell.dirty.test
 *
 * * Shell.dirty.test.tsx — Tests for content dirty tracking and beforeunload
 * Tests that pane:dirty-changed bus messages update contentDirtyPanes.
 * Tests that beforeunload handler is set when either dirty flag is true.
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 * - import { render, waitFor } from '@testing-library/react';
 * - import { Shell } from '../Shell';
 * - import type { ShellState } from '../../types';
 * - import { INITIAL_STATE } from '../../reducer';
 * - import { makeEmpty } from '../../utils';
 *
 * Components/Functions:
 * - initialState: TypeScript function/component
 * - initialState: TypeScript function/component
 * - mockEvent: TypeScript function/component
 * - initialState: TypeScript function/component
 * - initialState: TypeScript function/component
 * - mockEvent: TypeScript function/component
 * - initialState: TypeScript function/component
 * - initialState: TypeScript function/component
 * - initialState: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
