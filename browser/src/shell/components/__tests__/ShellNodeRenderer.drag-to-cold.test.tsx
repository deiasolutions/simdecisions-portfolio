/**
 * ShellNodeRenderer.drag-to-cold.test
 *
 * * ShellNodeRenderer.drag-to-cold.test.tsx — BUG-054 regression test
 * Verifies that COLD (empty) panes accept pane drags and dispatch MOVE_APP.
 *
 * Dependencies:
 * - import { describe, it, expect, vi, beforeEach } from 'vitest';
 * - import { render, screen, fireEvent, act } from '@testing-library/react';
 * - import React from 'react';
 * - import { ShellNodeRenderer } from '../ShellNodeRenderer';
 * - import { makeEmpty } from '../../utils';
 * - import type { AppNode } from '../../types';
 * - import { ShellNodeType, LoadState } from '../../types';
 *
 * Components/Functions:
 * - mockDispatch: TypeScript function/component
 * - mockBus: TypeScript function/component
 * - makeMockDataTransfer: TypeScript function/component
 * - store: TypeScript function/component
 * - makeColdEmptyNode: TypeScript function/component
 * - makeHotTerminalNode: TypeScript function/component
 * - node: TypeScript function/component
 * - el: TypeScript function/component
 * - node: TypeScript function/component
 * - coldPane: TypeScript function/component
 * - dt: TypeScript function/component
 * - dropzone: TypeScript function/component
 * - node: TypeScript function/component
 * - coldPane: TypeScript function/component
 * - dt: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
