/**
 * vite.config
 *
 * * Serve *.set.md files from browser/sets/ directory.
 * Single source of truth — no copies in browser/public/.
 * Dev: middleware serves from sets/ on request.
 * Build: copies sets/ into dist/ so production has them.
 *
 * Dependencies:
 * - import { defineConfig } from 'vite'
 * - import react from '@vitejs/plugin-react'
 * - import fs from 'fs'
 * - import path from 'path'
 *
 * Components/Functions:
 * - serveSets: TypeScript function/component
 * - setsDir: TypeScript function/component
 * - filename: TypeScript function/component
 * - filePath: TypeScript function/component
 * - outDir: TypeScript function/component
 * - sets: TypeScript function/component
 * - set: TypeScript function/component
 * - spaFallback: TypeScript function/component
 * - url: TypeScript function/component
 *
 * SOURCE AVAILABLE ON REQUEST
 * Contact: Dave Eichler — linkedin.com/in/daaaave-atx
 */

// This file is a stub. See README.md for full source access.
