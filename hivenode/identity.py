"""
identity
========

Unified node identity and database path resolution.

This module provides authoritative functions for node ID generation and
database path resolution. All consumers should use these functions to ensure
consistent identity and paths across the system.

Dependencies:
- import uuid
- import yaml
- from pathlib import Path
- from typing import Optional

Functions:
- get_node_id(): Get or generate node ID.
- get_device_id(): Get or generate device ID.
- get_db_path(db_name: str): Get canonical database path for a given database name.

SOURCE AVAILABLE ON REQUEST
Contact: Dave Eichler — linkedin.com/in/daaaave-atx
"""

# This file is a stub. See README.md for full source access.
