"""
schema
======

Event Ledger schema creation with WAL mode and indexes.

Dependencies:
- import sqlite3
- from pathlib import Path
- from .benchmark_events import BENCHMARK_EVENT_TYPES

Functions:
- create_schema(db_path: str): Create the Event Ledger schema with all columns, indexes, and WAL mode.
- migrate_schema(db_path: str): Apply schema migrations to existing databases.

SOURCE AVAILABLE ON REQUEST
Contact: Dave Eichler — linkedin.com/in/daaaave-atx
"""

# This file is a stub. See README.md for full source access.
