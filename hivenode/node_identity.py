"""
node_identity
=============

Node identity generation and persistence.

Dependencies:
- import secrets
- from pathlib import Path
- from typing import Optional

Functions:
- generate_node_id(): Generate a new node ID in format: node-{8hex}.
- get_or_create_node_id(base_dir: Optional[Path] = None): Get or create node ID, persisting to ~/.shiftcenter/node_id.

SOURCE AVAILABLE ON REQUEST
Contact: Dave Eichler — linkedin.com/in/daaaave-atx
"""

# This file is a stub. See README.md for full source access.
