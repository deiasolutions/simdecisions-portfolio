"""
system_channels
===============

System channels — auto-created on startup.

Ported from platform/efemera/src/efemera/channels/system_channels.py.
Uses relay store module-level functions (SQLAlchemy Core).

Dependencies:
- import logging

Functions:
- seed_system_channels(): Create system channels if they don't exist. Returns {name: channel_id}.
- create_personal_channel(user_id: str, display_name: str): Create a personal channel for a user if one doesn't exist. Returns channel ID.

SOURCE AVAILABLE ON REQUEST
Contact: Dave Eichler — linkedin.com/in/daaaave-atx
"""

# This file is a stub. See README.md for full source access.
